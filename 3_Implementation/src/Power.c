#include"Function.h"
  /**
 * @brief  finding  the Power of the number
 * 
 * @param A operand1
 * @param B operand2
 * @return int  it returns the output value
 */
int Power(int A ,int B)
{
  int power1;
    power1=pow(A, B);
    return power1;
}
